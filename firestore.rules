rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Émulateur local uniquement — ouvre tout pour le dev (ne pas réutiliser tel quel en prod)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
